services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: autoreparos-frontend
    restart: always
    environment:
      - API_URL=http://localhost:8080
    ports:
      - "4200:80"
